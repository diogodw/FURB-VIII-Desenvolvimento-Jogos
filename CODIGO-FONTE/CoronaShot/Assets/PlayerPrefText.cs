using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerPrefText : MonoBehaviour
{

    /*void Update()
    {
        GetComponent<Text>().text = PlayerPrefs.GetInt("Score").ToString("0000");
        PlayerPrefs.SetInt("Wave", (int)(PlayerPrefs.GetInt("Score") / 50) + 1);
    }*/
    
}
